<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8">
  <title>{{title}}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root{color-scheme:light}
    *{box-sizing:border-box}
    body{margin:0;font-family:"Segoe UI",Roboto,"Helvetica Neue",Arial,"Noto Sans",sans-serif;background:#f4f6fb;color:#1f2933;line-height:1.6}
    a{text-decoration:none;color:inherit}
    img{max-width:100%;display:block;border-radius:0.5rem}
    .navbar{background:#1f2933;color:#fff;padding:1rem 0}
    .navbar-inner{max-width:1200px;margin:0 auto;padding:0 1.5rem;display:flex;align-items:center;justify-content:space-between}
    .brand{font-weight:700;font-size:1.25rem;letter-spacing:0.04em}
    .nav-links{display:flex;gap:1rem}
    .nav-link{color:#fff;font-weight:500;padding:0.35rem 0.75rem;border-radius:999px;transition:background 0.2s ease}
    .nav-link:hover{background:rgba(255,255,255,0.16)}
    .layout{max-width:1200px;margin:0 auto;padding:2rem 1.5rem;display:grid;grid-template-columns:3fr 1.1fr;gap:2rem}
    .content{display:flex;flex-direction:column;gap:2rem}
    .sidebar{background:#fff;border-radius:1rem;padding:1.5rem;box-shadow:0 20px 45px -20px rgba(15,23,42,0.25);position:sticky;top:1.5rem;height:fit-content}
    .sidebar-title{margin:0 0 1.25rem;font-size:1.25rem;font-weight:700}
    .sidebar-section+.sidebar-section{margin-top:2rem}
    .sidebar-stats{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0.75rem;padding:0;margin:0;list-style:none}
    .sidebar-stats li{background:#eef2ff;border-radius:0.75rem;padding:0.75rem;font-weight:600;color:#3730a3;display:flex;flex-direction:column;gap:0.25rem}
    .sidebar-stats span{font-size:0.825rem;color:#4c51bf;font-weight:500}
    .sidebar-comments{margin:0;padding:0;list-style:none;display:flex;flex-direction:column;gap:1rem}
    .sidebar-comment{padding:0.75rem;border-radius:0.75rem;background:#f8fafc;border:1px solid #e2e8f0}
    .sidebar-comment strong{display:block;font-size:0.9rem;margin-bottom:0.35rem;color:#0f172a}
    .sidebar-comment small{display:block;color:#64748b;font-size:0.75rem;margin-bottom:0.25rem}
    .sidebar-empty{color:#475569;font-size:0.9rem}
    .btn-primary{background:#4f46e5;color:#fff;border:none;border-radius:999px;padding:0.5rem 1.1rem;font-weight:600;cursor:pointer;transition:transform 0.2s ease,box-shadow 0.2s ease;}
    .btn-primary:hover{transform:translateY(-1px);box-shadow:0 12px 25px -15px rgba(79,70,229,0.7)}
    .btn-secondary{background:#fff;color:#4f46e5;border:1px solid rgba(79,70,229,0.35);border-radius:999px;padding:0.45rem 1.05rem;font-weight:600;cursor:pointer;transition:background 0.2s ease,color 0.2s ease}
    .btn-secondary:hover{background:#eef2ff;color:#3730a3}
    .form-card{background:#fff;border-radius:1rem;padding:1.5rem;box-shadow:0 20px 45px -25px rgba(15,23,42,0.25);display:flex;flex-direction:column;gap:1rem}
    .form-card h2{margin:0;font-size:1.35rem;font-weight:700;color:#1f2933}
    .form-alert{background:#fee2e2;color:#b91c1c;border:1px solid rgba(248,113,113,0.4);padding:0.75rem 1rem;border-radius:0.75rem;font-weight:600}
    .form-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1rem;margin: 0.4rem 0;}
    .input-field{display:flex;flex-direction:column;gap:0.4rem;margin: 0.4rem 0;}
    .input-field label{font-size:0.85rem;font-weight:600;color:#475569}
    .input-field input,.input-field textarea{border:1px solid #cbd5f5;border-radius:0.75rem;padding:0.6rem 0.75rem;font-size:0.95rem;background:#f8fafc;color:#0f172a;transition:border 0.2s ease,box-shadow 0.2s ease}
    .input-field input:focus,.input-field textarea:focus{outline:none;border-color:#6366f1;box-shadow:0 0 0 3px rgba(99,102,241,0.18)}
    .input-field textarea{min-height:120px;resize:vertical}
    .hero{background:#4f46e5;border-radius:1rem;padding:2rem 2.25rem;color:#fff;display:flex;flex-direction:column;gap:0.75rem;box-shadow:0 25px 55px -30px rgba(79,70,229,0.7)}
    .hero h1{margin:0;font-size:2rem;font-weight:700;letter-spacing:0.02em}
    .hero p{margin:0;font-size:1rem;max-width:38ch;color:rgba(255,255,255,0.85)}
    .section-title{margin:0 0 1.25rem;font-size:1.5rem;font-weight:700;color:#1e1b4b}
    .image-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:1.5rem}
    .image-card{background:#fff;border-radius:1rem;overflow:hidden;box-shadow:0 20px 45px -25px rgba(15,23,42,0.35);display:flex;flex-direction:column;transition:transform 0.25s ease,box-shadow 0.25s ease}
    .image-card:hover{transform:translateY(-4px);box-shadow:0 30px 65px -35px rgba(15,23,42,0.45)}
    .image-meta{padding:1.2rem;display:flex;flex-direction:column;gap:0.75rem}
    .image-meta h3{margin:0;font-size:1.15rem;font-weight:700;color:#1e293b}
    .image-meta p{margin:0;font-size:0.95rem;color:#475569}
    .image-stats{display:flex;gap:0.75rem;font-size:0.85rem;font-weight:600;color:#475569}
    .image-stats span{display:flex;align-items:center;gap:0.35rem}
    .empty{margin:0;font-size:1rem;color:#64748b}
    .image-detail{display:flex;flex-direction:column;gap:2rem}
    .image-detail-header{display:flex;flex-wrap:wrap;align-items:center;justify-content:space-between;gap:1rem}
    .image-detail-title{margin:0;font-size:2rem;font-weight:700;color:#1f2937}
    .image-detail figure{margin:0;overflow:hidden;border-radius:1rem}
    .image-description{margin:0;font-size:1rem;color:#475569}
    .detail-actions{display:flex;align-items:center;gap:1rem}
    .like-count{font-weight:600;color:#4f46e5}
    .comment-section{background:#fff;border-radius:1rem;padding:1.75rem;box-shadow:0 20px 45px -25px rgba(15,23,42,0.25);display:flex;flex-direction:column;gap:1.5rem}
    .comment-header{display:flex;align-items:center;justify-content:space-between;gap:1rem}
    .comment-header h2{margin:0;font-size:1.5rem;font-weight:700;color:#1e1b4b}
    .comment-list{margin:0;padding:0;list-style:none;display:flex;flex-direction:column;gap:1rem}
    .comment-item{background:#f8fafc;border:1px solid #e2e8f0;border-radius:0.75rem;padding:1rem;display:flex;flex-direction:column;gap:0.5rem}
    .comment-meta{display:flex;flex-wrap:wrap;gap:0.75rem;font-size:0.85rem;color:#475569;font-weight:600}
    .comment-meta span{color:#94a3b8;font-weight:500}
    .comment-item p{margin:0;font-size:0.95rem;color:#1f2933}
    .comment-form{display:flex;flex-direction:column;gap:1rem}
    .footer{padding:2rem 0;background:#0f172a;color:#e2e8f0;margin-top:3rem}
    .footer-inner{max-width:1200px;margin:0 auto;padding:0 1.5rem;font-size:0.9rem}
    @media (max-width:1024px){
      .layout{grid-template-columns:1fr;gap:2.5rem}
      .sidebar{position:static}
    }
    @media (max-width:640px){
      .navbar-inner{flex-direction:column;align-items:flex-start;gap:0.75rem}
      .layout{padding:1.5rem 1.25rem}
      .hero{padding:1.5rem}
      .hero h1{font-size:1.65rem}
      .comment-header{flex-direction:column;align-items:flex-start}
      .detail-actions{width:100%}
    }
  </style>
  {{{head}}}
</head>
<body>
  <header class="navbar">
    <div class="navbar-inner">
      <div class="brand">ImageSpace</div>
      <nav class="nav-links">
        <a class="nav-link" href="/">首頁</a>
        <a class="nav-link" href="/images/1">精選圖片</a>
      </nav>
    </div>
  </header>
  <div class="layout">
    <div class="content">
      {{{body}}}
    </div>
    {{> sidebar}}
  </div>
  <footer class="footer">
    <div class="footer-inner">© {{currentYear}} ImageSpace 影像共享平台</div>
  </footer>
  <script src="/js/scripts.js" defer></script>
</body>
</html>
